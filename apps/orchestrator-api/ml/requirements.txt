# Standalone ML pipeline deps — runs OFF the Render orchestrator (locally / Mac
# mini / a worker), writes results to Supabase. Do NOT add these to the
# orchestrator's requirements.txt (Render 512MB free tier can't hold them).
python-dotenv>=1.0.1
psycopg2-binary>=2.9.9
SQLAlchemy>=2.0.31
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0
finance-datareader>=0.9.50      # import name: FinanceDataReader
pandas-ta>=0.3.14b0        # technical indicators
scikit-learn>=1.4.0
xgboost>=2.0.0
lightgbm>=4.3.0
joblib>=1.3.0
# realtime collector (rt_kiwoom port)
requests>=2.31.0
websocket-client>=1.7.0
