fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
redis==5.0.7
psycopg2-binary==2.9.9
python-dotenv==1.0.1
httpx==0.27.0
sqlalchemy==2.0.31
alembic==1.13.1
asyncpg==0.29.0
openpyxl==3.1.5
python-docx==1.1.2
python-pptx==1.0.2
pypdf==5.1.0
python-multipart==0.0.9
olefile==0.47
# fastembed — local ONNX multilingual embeddings (384-dim, ~120MB model, no API key).
# Re-added 2026-06-29 after Render was upgraded to >=1GB. Powers semantic RAG when
# EMBED_PROVIDER=local (Korean+English cross-language retrieval). Falls back to keyword
# (pgroonga) if the model can't load. Was pulled earlier because the 512MB free tier OOM'd.
fastembed>=0.8.0

defusedxml>=0.7.1

# YouTube transcript fetching for the youtube_report (no API key needed)
youtube-transcript-api>=0.6.2

# Full article-text extraction for the deep newspaper report
trafilatura>=1.8.0

# YouTube audio download for Whisper transcription (A+ youtube report)
yt-dlp>=2024.0.0

# M5.6 hourly AI model (ml/models_hourly/hourly_up.joblib) — WITHOUT these the model
# silently fails to load on Render and prob_up_1h() returns None everywhere (found
# 2026-07-09: 'AI %' showed as None on prod all day while working locally). Pinned to
# the training machine's versions so the pickled LogReg loads identically.
scikit-learn==1.8.0
joblib==1.5.3
pandas==2.3.3
numpy==2.4.6
