fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
redis==5.0.7
psycopg2-binary==2.9.9
python-dotenv==1.0.1
httpx==0.27.0
sqlalchemy==2.0.31
alembic==1.13.1
asyncpg==0.29.0
openpyxl==3.1.5
python-docx==1.1.2
python-pptx==1.0.2
pypdf==5.1.0
python-multipart==0.0.9
olefile==0.47
# fastembed pulled out — Render free tier (512MB) can't load the model.
# Default retrieval now uses pgroonga full-text search on Supabase. Add
# fastembed back here and set EMBED_PROVIDER=local if you upgrade Render to >=1GB.

defusedxml>=0.7.1

# YouTube transcript fetching for the youtube_report (no API key needed)
youtube-transcript-api>=0.6.2
