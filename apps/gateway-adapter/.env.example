# VIP Gateway Adapter — Environment Variables

# Orchestrator API URL (required)
ORCHESTRATOR_API_URL=http://localhost:8000

# CORS (comma-separated, empty = allow all)
CORS_ALLOWED_ORIGINS=

# Environment
APP_ENV=development
